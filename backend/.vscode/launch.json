{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python : FastAPI (uvicorn)",
      "type": "debugpy",
      "request": "launch",
      "python": "${workspaceFolder}/.venv/bin/python",
      "windows": { "python": "${workspaceFolder}/.venv/Scripts/python.exe" },
      "module": "uvicorn",
      "args": ["run:app", "--reload", "--port", "8000"],
      "subProcess": true,
      "justMyCode": false,
      "console": "integratedTerminal"
    }
  ]
}
